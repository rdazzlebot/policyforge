Development-only: the pinned `semgrep` version moves 1.176.1 to 1.177.0.
Nothing in the shipped package changes — `requirements/semgrep/` holds the
SAST tool's own environment and is not installed by users.
