"""Who the organization is: the tools it runs and the teams that own things.

Kept apart from `generate/` because it is not about drafting. The same
profile decides what a Procedure calls the identity provider, what the SSP
names as the system owner, and which placeholders `check` reports as still
outstanding — three consumers that should not have to agree on a format by
copying each other.
"""
