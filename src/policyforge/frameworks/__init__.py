"""Framework content: where it lives, and who may hold a copy of it.

Separate from `ingest/`, which is about parsing a catalog into this
project's schema. This is about the catalog as a *file with terms attached*
— which repository it may sit in, and whether committing it is a licence
breach. Those are different questions with different consequences.
"""
