"""Package data: the redistributable catalogs and example configs.

`frameworks/` and `config/` are mapped here from `data/frameworks/` and
`config/` by pyproject.toml. See `policyforge.scaffold`.
"""
